## Packages
framer-motion | Essential for smooth animations and the evasive "No" button
canvas-confetti | For the celebration effect when "Yes" is clicked
@types/canvas-confetti | Types for confetti

## Notes
- The "No" button needs complex interaction logic (running away on hover/tap).
- We need to track the number of attempts to show the "nervous" text.
- We need to send the final response to the backend via the API.
